[build]
  command = "pnpm install --no-frozen-lockfile && pnpm run build"
  publish = "dist"
  environment = { VITE_API_URL = "https://your-api-url" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
